{
  "name": "services",
  "scripts": {
    "start:auth": "npm run start --workspace=authService",
    "start:accounts": "npm run start --workspace=accountsService",
    "start:products": "npm run start --workspace=productsService",
    "test": "npm test --workspaces --max-old-space-size=8192"
  },
  "workspaces": [
    "accountsService",
    "authService",
    "productsService"
  ],
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
